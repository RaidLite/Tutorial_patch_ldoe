Patch this in GameManager class only!

patch 1.0:

Awake() add this.Server = false; before Init()
change battle_url and main_url to "offline://"

patch 1.1:

Awake() add this.Server = false; before Init()
in _startUrl change links to only "https://raw.githubusercontent.com/RaidLite/ConfigServer/refs/heads/main/ldoez_config.json"

patch 1.2+:

in _startUrls change links to only "https://raw.githubusercontent.com/RaidLite/ConfigServer/refs/heads/main/ldoez_config.json"
Awake() add this.Server = false; before Init()

in 1.4.1 kefir started using il2cpp for all new versions !!!
